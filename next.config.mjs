import { Domain } from "domain";

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'links.papareact.com' ,'mobileimg.priceline.com', 'images.remotePatterns'],
  },
};

export default nextConfig;
